// All mock data is now programmatically generated and loaded into the SQL.js database
// within the `services/db.ts` file. This file is kept to prevent import errors,
// but the static arrays for customers, products, and orders have been removed
// as they are superseded by the more complex schema.
